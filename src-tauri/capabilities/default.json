{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions the Nyra window needs. Filesystem, process and shell access all go through our own commands rather than the generic plugins, so none of those capabilities are granted here.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-show",
    "core:window:allow-set-focus",
    "core:window:allow-start-dragging",
    "core:window:allow-toggle-maximize",
    "core:event:default",
    "dialog:default",
    "opener:default",
    "notification:default",
    "process:default"
  ]
}
