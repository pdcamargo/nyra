<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <key>com.apple.security.app-sandbox</key>
    <false/>
    <!--
      Voice dictation. Under the hardened runtime the usage description in
      Info.plist supplies the sentence the prompt shows; this entitlement is
      what allows the prompt to happen at all. The grant survives rebuilds
      because scripts/build-app.mjs signs with a real certificate, so the
      designated requirement names the certificate rather than a cdhash.
    -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
</dict>
</plist>
